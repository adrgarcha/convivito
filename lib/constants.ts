export const { BOT_ID, ACCESS_TOKEN, WEBHOOK_VERIFY_TOKEN, TEST_RECIPIENT, REDIS_HOST, REDIS_PORT, REDIS_PASSWORD, NODE_ENV } = process.env;

export const DAYS = ['lunes', 'martes', 'miércoles', 'jueves', 'viernes', 'sábado', 'domingo'];
